// Dependencies ---------------------------------------------------------------
import { Data } from "./index";

// Collection+JSON interface implementations ----------------------------------
export interface Template {
    data: Data[]
}